# Getting Started

To consume this library as part of a cmake project add this repo as a submodule and add the top level directory with `add_subdirectory` to your cmake file.

Link the target `scheduler`.
